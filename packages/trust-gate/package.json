{
  "name": "@aidprotocol/trust-gate",
  "version": "1.0.0",
  "description": "GitHub Action — check AID trust score before deploying",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.mjs",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "aid", "trust", "github-action", "ci-cd", "agent", "deploy-gate",
    "trust-scoring", "security"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/aidprotocol/trust-gate"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
