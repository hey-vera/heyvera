# Packet 1 Exec

Use this file for the actual Packet 1 model run.

## Goal

Re-establish or improve the public-site foundation only.

## Edit Targets

Prefer editing only:
- `src/App.tsx`
- `src/index.css`

Create or edit `src/components/*` only if Packet 1 truly needs a small
shared layout component.

## Scope

- global visual tokens
- type direction
- page background and atmosphere
- layout rhythm
- shared section container patterns

## Do Not Do

- do not add later sections
- do not add routes or pages
- do not add signed-in app UI
- do not add filler copy
- do not hand-edit `frontend-sync/STATUS.md`

## Done Looks Like

- the page already feels intentional before full content exists
- the base layout feels premium and calm
- tokens and spacing live in reusable styles, not one giant blob

## Packet Prompt

Use a short prompt like:

`Build only Packet 1 from this file. Edit only the minimum files needed. Stop after the packet.`
