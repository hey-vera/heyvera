export function JoinBar() {
  return (
    <div className="join-bar">
      <span className="join-bar-text">
        Start in Home — read the public feed, then sign in from the shell when you are ready.
      </span>
      <a className="button button-primary join-bar-btn" href="#feed">
        Open Feed
      </a>
    </div>
  );
}
