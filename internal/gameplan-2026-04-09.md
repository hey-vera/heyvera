# Gameplan — 2026-04-09: Soma Production Ready

**Goal:** Get Soma ready to profit. ClawAPIs.com is the first customer (provider). By end of day, every component should work end-to-end with real data.

---

## Priority 1: Soma Integration Testing (Real OpenClaw Scenarios)

Test the full lifecycle with real-ish API calls against clawapis.com endpoints:

1. **Provider registration flow** — clawapis registers, gets birth cert, endpoints seeded
2. **API call → Heartbeat** — direct call to an endpoint, verify Pulse Tree leaf is appended
3. **Soma Check (x402 ETag)** — call endpoint, get hash, call again with `If-Soma-Hash`, verify 304 skip-payment
4. **Trust Oracle query** — query trust for clawapis agent DID at basic/dimensional/full tiers, verify scoring works
5. **Vouch graph** — stake a vouch for clawapis, verify social dimension changes
6. **Credit metering** — verify trust query charges credits correctly, provider revenue share on API calls
7. **Risk flags** — test LOW_ACTIVITY → normal transition as actions accumulate
8. **Birth cert verification** — verify certs via sense-observer package

**Fix any gaps found.** This is the "make it real" pass.

## Priority 2: Ultra-Think — Soma Unification Question

Run ultra-think analysis on: **Should soma check + ClawNet caching + routing all merge under "Soma"?**

Consider:
- Is "Soma" the protocol and "ClawNet" the implementation? Or is everything Soma?
- What does the brand architecture look like externally?
- Does merging simplify or confuse the pitch?
- Review/edit the ultra-think prompts if needed before running

## Priority 3: Nova Prover Build on VPS

- Bump VPS to 8GB
- Add 4GB swap
- Install Rust
- `cd claw-net/prover && CARGO_BUILD_JOBS=2 cargo build --release`
- Fix any Sonobe/arkworks version pin issues from compiler output
- Verify prover starts and responds to `ping` IPC command

## Priority 4: Smart Contract Prep Note

Not building yet, but documenting the path:
- Sonobe's `solidity-verifiers` crate auto-generates a Groth16 verifier contract
- Deploy target: Base (L2)
- This is Receipt Layer Phase 1 — after Soma is proven in production
- Learn Solidity basics before this phase (Hardhat, deploying to Base testnet)

## Stretch Goals (if time permits)

- Dashboard: trust score visualization for provider portal
- Pulse marketing agent updates (X.com integration)
- Landing page / docs updates for new trust oracle + vouch system
- Review gap-analysis-2026-04-07.md and close any items

---

## Conversation Starter for Tomorrow

```
Hey Opus, we're continuing the ClawNet/Soma build. Today's goal: get Soma
production-ready for clawapis.com as our first customer. The gameplan is in
internal/gameplan-2026-04-09.md — start with Priority 1 (integration testing
with real scenarios), then we'll decide on the Soma unification question.
Everything we built yesterday (trust oracle, vouch graph, free routing,
Nova prover) needs to work end-to-end.
```

## What "Production Ready" Means

- [ ] A provider can register and their endpoints get Soma identity (birth certs)
- [ ] API calls through ClawNet generate Pulse Tree entries
- [ ] Soma Check saves providers money on unchanged data
- [ ] Trust Oracle returns meaningful scores for active agents
- [ ] Trust queries charge credits (our revenue)
- [ ] Vouch graph lets agents stake trust in each other
- [ ] No crashes, no silent failures, no uncaught edge cases
- [ ] ClawAPIs can go through the full flow and it just works
