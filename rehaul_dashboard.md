# Rehaul: dashboard.html — COMPLETED

**Status:** DONE. Fully rewritten with new design system (~800 lines).

Includes Task History table (`GET /v1/tasks?limit=10`), Usage Breakdown (`GET /v1/auth/usage`), all modals, USDC payment flow, and Clerk auth. No action needed.
